/* Empties the slot when the router lands back on /listings, so the panel does not stay
   on screen after Back. Parallel slots keep their last match otherwise. */
export default function DrawerClosed() {
  return null;
}
